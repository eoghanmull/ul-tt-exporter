extends layout

block content
  center
    div(class="input_field")
      div(class="input-field col s6")
        input(placeholder="Please insert your student ID" id="S" type="text" class="center")
        label(for="first_name") student ID
        div(id="submit")
          button(class="btn waves-effect waves-light" type="submit" name="action" onclick="submitBtn()") Submit
            i(class="material-icons right") send

